name: PreProcessTextData
description: |
  Prepare data for PyTorch training.

inputs:
  - name: input_data
    description: 'The path to the input dataset'
    type: String
  - name: dataset_url
    description: 'The path to the dataset file'
    type: String
  
outputs:
  - name: output_data
    description: 'Output data location'
    type: String
 
implementation:
  container:
    image: jagadeeshj/data_prep_step:24-02-2021-21-28-48.600572
    command: ["python","/component/entry_point.py"]
   
    args: [
      --input_data, {inputPath: input_data},
      --dataset_url, {inputValue: dataset_url},
      --output_data, {outputPath: output_data}
    ]
   